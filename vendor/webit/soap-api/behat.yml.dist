default:
    suites:
        global_weather:
           paths:
               -  %paths.base%/features/global-weather.feature
           contexts:
               - GlobalWeatherContext:
                   mockSoapClient: true
        ip2geo:
           paths:
               -  %paths.base%/features/ip2geo.feature
           contexts:
               - IP2GeoContext:
                   mockSoapClient: true
